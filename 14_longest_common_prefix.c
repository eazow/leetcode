#include <assert.h>

char *longestCommonPrefix(char** strs, int strSize) {
    if(strsSize <= 0) 
        return NULL;
    char *str = strs[0];
    int max = len(str);

}

int main() {
    


    return 0;
}
